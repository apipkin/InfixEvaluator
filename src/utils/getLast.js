/**
 * Shortcut to get the last element in an array
 * @param {array} Array to inspect and return
 * @return {any}
 */
function getLast(arr) {
  return arr[arr.length - 1];
}

module.exports = getLast;
